apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      # Let Grafana display the Prometheus-managed alert rules (read-only).
      manageAlerts: true
      httpMethod: POST
